# Pesquisa 3.0

Este repositório contém a versão web em **Next.js** do sistema de pesquisas.

## Estrutura

- `pesquisa-3/`: aplicação principal (Next.js + Supabase + Prisma).

## Como rodar

```bash
cd pesquisa-3
npm install
npm run dev
```

Acesse `http://localhost:3000`.
